# 文件传输与剪贴板同步需求文档（M25）

> 状态：**需求草案，未开工**。2026-09-03 依据两端源码现状核实后撰写。
> §8.3 有**一项待使用者拍板**（隐藏托盘图标会把本功能的宿主进程杀掉）。
> 基线：`main` = `04877bb`，已发布 `v1.0.7`；本文只定需求，不含代码改动。

## 0. 一句话目标

在**不新增任何网络监听、不破坏单向隧道不变量**的前提下，让控制端与被控端之间：① 文本/图片剪贴板双向互通；② 一端复制文件、另一端粘贴即按需传输；③ 一个"文件传输"按钮打开双栏浏览器做显式双向传输。

## 1. 现状缺口

| 能力 | 现状 | 影响 |
|---|---|---|
| 剪贴板同步 | 完全没有（两端互不可见） | 跨机传一段文字要靠截图或邮件 |
| 文件跨机 | 完全没有 | 小文件靠 U 盘/IM，违背"隔离内网运维"的初衷 |
| 键鼠透传 | 有，但 Ctrl+C/V 无特判（`server/src/input_gateway.cpp:178`） | 复制粘贴动作已经"真的在对端执行了"，拦截点必须新加 |
| 隧道流控 | 无分片、无多路复用、无背压 | 大文件与画面抢同一条 socket，必须先改造发送路径 |
| 断线续传 | 全库无 chunk/offset/resume 任何概念 | 见 §7.5 的"不做续传"最小语义 |

## 2. 已拍板的决策（含被砍掉的选项与代价）

| # | 决策 | 被砍掉的替代 | 已知代价 |
|---|---|---|---|
| D1 | **延迟渲染**：粘贴那一刻才拉数据，复制时只公布清单 | 复制即预传 | 需惰性 OLE 数据对象、跨完整性级别调用有不确定性（§11 R1） |
| D2 | **文本/图片剪贴板同期做** | 本期只做文件 | 需回环抑制与所有权丢失处理（FR-1、§11） |
| D3 | 面板 = **双栏浏览 + 双向传输 + 队列进度** | 拖拽、新建/重命名/删除 | 面板不是一个文件管理器 |
| D4 | 护栏**只要求"传输方向可配置"** | 被控端弹窗确认、审计日志、人为速率档 | 被控端使用者看不到"谁在传什么"（§11 R6 记为已知情接受） |
| D5 | 被控端 provider **复用现成 user-IL 托盘代理** | 另起专用 helper / 控→被方向降级预传 | `HANDOFF §4.1` 读写互锁缺陷必须先修（§13 M25-0） |
| D6 | **文件夹本期明确拒绝**并给出确定原因 | 递归展开平铺 / 打包 zip | 从资源管理器复制一个夹子不可用（FR-2.4） |
| D7 | **自动让路**：文件分片为最低优先级，画面与输入永远优先 | "优先传输"手动开关 / 不处理 | 传大文件时文件本身达不到链路峰值速率 |

## 3. 术语与角色

- **控制端** = `control_server.exe`（Qt6 Widgets，普通桌面进程，非提权，会话级单实例）。
- **被控端** = `agent.exe`：服务（LocalSystem）→ **会话宿主**（SYSTEM 身份，负责画面与 SendInput，`client/src/service.cpp:89-139`）→ **托盘代理**（交互登录用户身份，每会话一份，`client/src/tray_proxies.cpp:356,372`）。
- **origin** = 一次传输的发起端；**sink** = 接收端。
- **方向以被控端为原点描述**：`in` = 进入被控端，`out` = 从被控端出去。

## 4. 硬性约束（违反一条即不予验收）

1. **禁止新增监听/端口/socket**：被控端全进程 TCP 监听数必须仍为 **0**，唯一服务端仍是命名管道 `MyRemoteAgent_TrayProxy_v1`（既有不变量，`tools/harness/m24_invariants.ps1` 当场数）。
2. 所有新消息复用既有反向隧道，**必须装在 `Encrypted(0x10)` AES-128-GCM 信封里**（唯一允许明文的是 `Heartbeat`）。
3. **画面与输入不得饿死**：传输进行中，控制端 GUI 线程单次停顿 < 200ms；被控端输入注入到 `SendInput` 的延迟不得因传输上升（判据 A3/A4）。
4. **托盘管道不承载文件字节**：行式 JSON、队列上限 32 行（`client/src/tray_proxies.cpp:42`），只允许控制信令；数据一律走隧道。
5. **混合版本必须天然降级**：新能力经 `Register` 尾字节 flags 协商（新增 `kFlagClipboardSync=0x40`、`kFlagFileTransfer=0x80`，沿用 `common/include/messages.hpp:52-59` 的"同一字节、热更新复用 `StateReport`"约定）。老端不置位 ⇒ 新消息永不下发；两端未知类型只 warn 不断线（`client/src/main.cpp:819`、`server/src/tunnel_manager.cpp:319`）双保险。
6. **策略由被控端强制**，控制端只负责显示与灰化，绝不作为唯一执行点（FR-5.3）。
7. 新键进 `ClientConfig`（现仅 7 键，`common/include/config.hpp:20-41`）必须同步过 `--set-server` 的"只改显式项、其余原样保住"与配置窗全键回写；非法值退回默认并 `mlog::warn`，**不许静默当 `both`**。
8. **发送路径改造是前置，不是附带优化**：现状 `TunnelManager::send_to_device` 由 Qt GUI 线程直接阻塞写（`server/src/tunnel_manager.cpp:487-499`，调用点 `server/src/remote_controller.cpp:162-416` 全在 GUI 线程），而 accept 出来的 socket **只设了 `SO_RCVTIMEO`、没有 `SO_SNDTIMEO`**（`:93-95`）——一条大帧能把整个控制端界面钉死。

## 5. 功能需求

### FR-1 文本/图片剪贴板双向互通

1. 任一端剪贴板变化 → 向对端公布摘要清单；对端按需拉内容。**文本上限 8 MiB、图片上限 32 MiB**，超限只公布摘要、UI 显示"内容过大未同步"。
2. 支持格式：`CF_UNICODETEXT`、`CF_DIB`/`CF_BITMAP`。对端应用索取本地没有的富格式（HTML/RTF）时不提供、不伪造。
3. **回环抑制**：本端因"接收对端内容"而写自己剪贴板时，必须吃掉随之而来的那一次更新通知，禁止 A→B→A 振荡。
4. **所有权被抢即停**：对端应用或 `rdpclip.exe` 抢走剪贴板所有权 ⇒ 放弃本次、不重试、不弹窗。
5. 安全桌面/锁屏：`kFlagSecureDesktop | kFlagLogonScreen` 置位时**冻结**同步（不丢弃），会话状态行显示"对端在安全桌面"。

### FR-2 文件跨端复制粘贴（延迟渲染）

1. **复制侧**：监听本端剪贴板 → 发现 `CF_HDROP` → 生成 manifest（相对路径、字节数、mtime）→ 只公布清单，**不传字节**。
2. **粘贴侧**：本端剪贴板放一个**惰性数据对象**，同时报 `CF_HDROP` 与 `FileGroupDescriptorW`/`FileContents(IStream)`。目标应用真去取时才触发按需拉取。
3. **拉取过程必须可见且可取消**：进度归属**粘贴侧**进程（控制端 = Qt 进度对话框；被控端 = 托盘代理自绘的 topmost 小窗，先例 `client/src/config_gui.cpp:106-135`）。取消/超时 ⇒ `GetData` 返回失败，表现为目标应用"粘贴没反应"，**不弹错误框**，只在会话状态行留一行原因。上限 60s。
4. **文件夹**：清单里出现目录 ⇒ 该次不传，状态行显示"暂不支持文件夹，请逐个选择文件"；面板里选中文件夹时「→」置灰并给同一原因。
5. **幂等**：同一清单被多次 `GetData`（应用普遍会问好几轮）⇒ 首次落盘后返回同一批路径，不重复传输。
6. **多文件平铺**：一次复制 N 个文件 = N 项传输队列条目，按选择顺序。
7. **落地位置由粘贴方决定**：本端只负责保证暂存文件对交互用户可读，不参与目标应用的写入与命名冲突处理。

### FR-3 "文件传输"按钮与双栏浏览器

1. 控制端 `SessionToolbar` 每会话一枚「文件传输」（现有按钮仅 3 枚，`server/src/session_toolbar.cpp:78-93`）。接线照 `logon_button_` 先例：toolbar signal → `server/src/session_view.cpp:91-100` → `RemoteController` → `TunnelManager`；图标由 `server/src/icon_factory.cpp` QPainter 现画。
2. 打开后**左栏本机、右栏该会话对端**；每栏可进目录、回上级、回盘符根。列目录一次一层，**禁止整树下发**（单次应答 ≤ 200 条，超出提示"过多"）。
3. 选中文件 → 「→」入队并立即开始传输；「←」反向。多选 = 多个平铺条目。
4. 非活动/断开的会话标签不允许打开面板；对端不具备能力（老版本）时按钮存在但置灰 + tooltip 说明原因。

### FR-4 传输队列与进度

1. 全局一个队列表（跨会话），列：文件、大小、方向、进度、状态、操作（取消/重试）。
2. 同时最多 **1 项在传**，其余排队（D7 的自然结果，不引入并发调度复杂度）。
3. 完成项保留到面板关闭；失败项标失败原因并可"重试"（= 新 transfer id 从头传）。

### FR-5 传输方向可配置

1. 被控端配置新增 2 键：`clipboard_direction`、`file_xfer_direction`，取值 `off|in|out|both`，**默认 `both`**（老配置无此键即保持现行为）。`in` = 只许进入被控端（即"禁止外发"，涉密内网的典型形态）。
2. 配置入口：被控端配置窗下拉 + `--set-server` 可写。
3. 强制点在**被控端每一条新消息的分支入口**（`client/src/main.cpp:819` 那个 switch），违反即回 `XferClose(reason=policy)` / `FsListReply(err=denied)`，**不看控制端脸色**。
4. 控制端不新增配置；首次被拒后灰掉对应方向按钮，tooltip 显示"对端策略禁止（由被控端强制）"。

### FR-6 右键复制

需求语义 = "在资源管理器/桌面用右键『复制』，与 Ctrl+C 完全等价"。因右键动作发生在**对端应用内部**、我们不经手，只要 FR-1/FR-2 的剪贴板监听是真的，右键即自动可用 ⇒ **不单独立项**。唯一硬要求：监听必须**事件驱动**（`AddClipboardFormatListener` / `QClipboard::dataChanged`），**禁止轮询**，否则右键与键盘两条路径不等价。控制端"文件传输"面板内的右键菜单（传送到对端）列入本期，作为按钮的等价入口。

## 6. 协议扩展（0x18~0x20，全部大端，照 `common/include/messages.hpp:124-183` 风格声明 make_/parse_）

```
ClipboardPeerState = 0x18  双向  [2B seq][1B fmt_mask][2B summary_blen][summary]
                                 [1B file_count]([8B size][2B relpath_blen][relpath])*
                                 fmt_mask: bit0 text / bit1 image / bit2 files
ClipboardGet       = 0x19  双向  [2B seq][1B fmt]
ClipboardData      = 0x1A  双向  [2B seq][1B fmt][2B len][bytes]        len ≤ 8MiB
FsList             = 0x1B  S→C   [4B req_id][2B path_blen][path]       path 空 = 盘符列表
FsListReply        = 0x1C  C→S   [4B req_id][2B err][2B parent_blen][parent]
                                 [2B count]([1B type][8B size][8B mtime_fticks][2B name_blen][name])*
                                 type: 0 folder / 1 file / 2 drive     count ≤ 200
XferOpen           = 0x1D  双向  [4B tid][4B epoch][8B total][2B relpath_blen][relpath]
XferData           = 0x1E  双向  [4B tid][8B offset][2B len][bytes]    单片 ≤ 64KiB
XferAck            = 0x1F  双向  [4B tid][8B next_offset][4B credit]
XferClose          = 0x20  双向  [4B tid][1B reason][2B msg_blen][msg]
```

- `relpath` 含 `/` 即代表子目录 ⇒ 一律 `reason=notFolder`（D6）。
- `reason`：`0 ok / 1 cancel / 2 policy / 3 notFound / 4 writeFail / 5 peerGone / 6 tooBig / 7 badName / 8 notFolder / 9 timeout`。
- `FsListReply.err`：`0 ok / 1 denied / 2 notFound / 3 notReady / 4 tooMany`。
- 单片取 64KiB 而不是顶到 64MiB：让 P2 分片天然细碎，出队器能在两片之间插进一帧视频。
- 字符串一律 UTF-8 上线，两端各自 `MultiByteToWideChar`/`QString::fromUtf8` 转回宽字符；路径含非法字符 ⇒ `badName`，不猜。

## 7. 传输引擎

1. **双端共用同一份实现**：新增 `common/include/transfer.hpp` + `common/src/transfer.cpp`。这是最大的风险收敛点，也符合本项目"一支判据量两端"的文化。
2. `tid` 由发起方自 1 单调分配；`epoch` 每次 `Register` 成功自增。
3. **信用流控**：sink 用 `XferAck(credit)` 决定在途字节数（初始 1 MiB、上限 4 MiB），origin 无信用不发片 ⇒ 内存有上界，且不把 `SO_SNDBUF` 灌满去挤视频。
4. **暂存目录**：被控端 `%ProgramData%\MyRemote\Xfer\<tid>\<name>.part`；控制端取 `server/src/app_paths.cpp` 同级。**ACL 必须在写入前设好**（SYSTEM 建目录即授交互用户读），写完 `MoveFileEx` 原子改名——事后补 ACL 不算数，粘贴方读不到就是静默空粘贴。
5. **断线语义（不做续传，D1+D7 的直接后果）**：`epoch` 变化 ⇒ 该 tid 整项作废、标失败、删 `.part`，进程启动时清扫遗留。**不自动重开**，只有人按"重试"。代价：跨 30s `Reconnecting` 宽限窗口的长传输必失败一次。
6. 取消：任一侧 `XferClose(cancel)`，双向确认后删 `.part`。
7. 完整性：`XferClose(ok)` 要求 `next_offset == total` 且落盘字节数一致，否则 `writeFail`。**MVP 不加哈希**——清单里带 mtime + size 已够人眼核对，加哈希只会多一处"算了却没人比"的假安心。

## 8. 两端进程形态与职责划分

### 8.1 每端新增一条专职发送队列线程（socket 写只归它）

- 控制端改造点：`server/src/tunnel_manager.cpp:487-499`；**并给 accept 出来的 socket 补 `SO_SNDTIMEO`**（`:93-95` 现在只有 `SO_RCVTIMEO`）。
- 被控端改造点：`common/src/connection.cpp:96-104` 与 `client/src/main.cpp:1112`（`SO_SNDTIMEO=2000` 会让每个分片最坏阻塞 2s，视频与 `queue_input` 一起饿死）。
- 三级出队：**P0** `InputEvent / Heartbeat / Ping / Pong / XferClose / XferAck / StartStream / StopStream` > **P1** `VideoFrame`（沿用"有更新帧就丢旧帧"，对齐 `server/src/frame_pipeline.cpp:65-74`）> **P2** `XferData`（受信用，且每写满 256KiB 主动让路一次）。P0/P1 队列满 ⇒ 断开走重连；P2 满 ⇒ 只停发分片。
- 刻意**不碰**：`session_loop`、`FrameDecoder` 缓冲上限、心跳参数、`kReconnectGraceMs=30000`。

### 8.2 控制端 provider

`QMimeData` 子类重写 `hasFormat/formats/retrieveData` 即得惰性渲染（Qt 的 `OleSetClipboard` 已在 GUI 线程 `OleInitialize`，全仓现有 `CoInitialize/OleInitialize` 命中数为 0）。硬要求：

1. `retrieveData` 跑在 GUI 线程，**绝不能同步等网络**——内部跑嵌套 `QEventLoop` + 模态进度框。
2. 重入：嵌套循环期间同一清单的 `hasFormat/formats` 会再次进来，加 in-flight 标志返回缓存。
3. `urls` 与 `text/uri-list` 都要报，否则只有一部分应用认。

### 8.3 被控端 provider = 托盘代理（user-IL，D5）

- SYSTEM 会话宿主继续独占 TCP、独占文件落盘；托盘代理只当**惰性数据对象宿主 + 本端剪贴板监听者**，与控制端侧对称（同为 Medium IL，out-call 不跨完整性级别）。
- 与控制端之间仍走现成命名管道，**只发控制信令**；字节走宿主暂存文件。
- **⚠ 待拍板（§11 R3）**：既有语义里「隐藏托盘图标」会让代理**自退**（`HANDOFF` §3 现场七条判账⑥：宿主 `no icon; retiring 1` → 代理 `the host hid the icon; exiting`）。照原样做，隐藏图标的机器上本功能静默不可用。**建议**：代理存活条件从"`tray_icon` 想要"扩成"`tray_icon` 想要 **或** `clipboard_direction`/`file_xfer_direction` 任一非 `off`"，隐藏只隐藏图标、代理以无图标形态继续存活。此项确认后才写死。

## 9. 非功能需求

- 传输速率：不承诺峰值（D7 让路的直接结果），只承诺"画面仍可看、鼠标仍跟手"。
- 内存：单端在途缓冲 ≤ 8 MiB（§7.3 信用上限）；判据需采样 RSS 上界。
- 上限：剪贴板文本 8 MiB / 图片 32 MiB；面板单目录 200 条；**单文件大小不设人为上限**（64MiB 帧上限 + 分片机制已是硬界），但要能给出 `tooBig` 错误码路径。
- 无 UI 阻塞：任何一次 `send / recv / 列目录` 都不得出现在 GUI 线程的同步调用栈上。

## 10. 本期明确不做

拖拽进出面板；新建/重命名/删除等远端管理操作；**文件夹与递归**；断点续传；传输审计日志；被控端逐次确认；符号链接与 `\?\` 长路径；富格式（HTML/RTF/PDF）剪贴板；跨平台；文件面板与多监视器的相互影响（多显示器本身仍在 backlog，`TASKS.md:157`）。

## 11. 风险（按会不会推翻设计排序）

- **R1｜跨完整性级别的惰性数据对象可能根本调不通**（Medium IL 的 explorer 反向 out-call 一个 SYSTEM provider）。这是 D1 的直接后果，**必须先探针定案再写代码**（判据 A2）。若探针证明不通，D5 的托盘代理形态正是解药（它已是 Medium IL），风险降级为实现细节，不推翻 D1。
- **R2｜被控端剪贴板监听该由 SYSTEM 主机还是 user-IL 代理做**：同会话剪贴板站两者都可访问，但 RDP 会话里 `rdpclip.exe` 会抢所有权。定案靠 A2/A5 探针，不靠推理。
- **R3｜「隐藏托盘图标」会杀掉 provider**（§8.3 待拍板）。
- **R4｜`HANDOFF §4.1` 读写互锁**：托盘管道的读线程与写线程共用一个非重叠句柄，对端不读则该代理命令全丢，失败方式是"静默吞掉一次用户点击"。既然 D5 把新能力压在这条管道上，**这条已确诊缺陷从"要不要修的决定"升格为 M25 前置**。
- **R5｜控制端 GUI 钉死**（§4.8）：不改造发送路径就上分片 = 一传文件整个界面点不动，且事后极难归因。
- **R6｜已知情砍掉的护栏**：无审计日志，被控端看不见也无法中止正在进行的传输。缓解仅剩"方向可配置"与托盘既有「停止远程控制」。此条须原样写进 release notes 已知限制。
- **R7｜`GetData` 阻塞粘贴方 UI**：目标应用（explorer/Office）在自己的 UI 线程上等网络，大文件表现为"那个应用没响应"。延迟渲染的固有形态，进度窗能缓解不能消除。
- **R8｜队头阻塞的反向情形**：被控端往控制端推分片时若 `SO_SNDBUF` 被灌满，视频发送失败即丢帧（`client/src/main.cpp:1112-1120`）——信用流控必须双向都生效，不只是"下载方向"。

## 12. 验收判据（脚本进受跟踪的 `tools/harness/`，纯 ASCII）

> 文化照旧：断言必须能真失败、判效果不判自报、一把秒表、要反向对照、结果文件自己写、别把仪器超时当产品门限。

| # | 判据 | 断言 |
|---|---|---|
| A0（前置） | R4 已修 | `m24_wedge.ps1` 三阶段 A/B/C 复跑全绿 |
| A1 反向对照 | 探针不说谎 | 所有新针先在 **1.0.7 旧镜像**上跑一遍，必须全 False，否则判据作废 |
| A2 跨 IL 探针 | R1 定案，**写代码之前跑** | SYSTEM 侧 `OleSetClipboard` 一个"仅在 `GetData` 时写日志并返回哨兵串"的数据对象；Medium IL 消费进程 `OleGetClipboard→GetData`。预期断言**读不到哨兵**；反向（provider 改 Medium IL 时读到）作为仪器自证 |
| A3 GUI 不冻结 | §4.3 / R5 | 控制端 GUI 线程 50ms `QTimer` 看门狗，传 2 GiB 期间最大间隔 < 200ms。**未改造版必须红** |
| A4 输入不饿死 | §4.3 | 传输期连发 `Ping`，量 `Pong` RTT p95 与基线差；同时鼠标移动仍被注入（对被控端读 `SendInput` 计数，不读自报） |
| A5 粘贴按结果断言 | FR-2 端到端 | 从 YEUNG 复制 sha256 已知的文件 → 在被控端粘贴到 `C:\xfer-test\` → 断言 `\\10.60.254.153\c$\xfer-test\<name>` 真存在、字节数一致、**属主/ACL 允许交互用户读**。日志只作辅助 |
| A6 策略单端强制 | §4.6 | TEST-WIN 配 `file_xfer_direction=in`，YEUNG 侧脚本硬发 `out` 方向消息 ⇒ 收到 `reason=policy` 且对端磁盘**没有**新文件；再证绕过 GUI 直发也拦得住 |
| A7 回环抑制 | FR-1.3 | 两端各粘一次文本 ⇒ `ClipboardPeerState` 的 seq 不振荡、10s 内消息数有界、CPU 不涨 |
| A8 文件夹明确拒绝 | D6 | 复制一个含 3 个文件的夹子 → 粘贴 ⇒ 无任何落地文件、状态行有"暂不支持文件夹" |
| A9 所有权被抢 | FR-1.4 | 粘贴前用第三方进程抢剪贴板 ⇒ 60s 内放弃、不崩、不重试风暴 |
| A10 断线不自动续传 | §7.5 | 传输中拔网 40s ⇒ 两端 `.part` 均被清理、无自动重开、队列项显示"失败·可重试" |
| A11 外部面不变 | §4.1 | `m24_invariants.ps1` 复跑：TCP 监听仍 0、管道无新动词 |
| A12 混合版本 | §4.5 | 老控制端(1.0.7)连新被控端 ⇒ 隧道不断、按钮置灰、日志无 unknown-type 风暴 |

## 13. 里程碑切分（需求确认后按序执行）

| # | 内容 | 完成判据 |
|---|---|---|
| M25-0 | **前置**：`HANDOFF §4.1` 三行 `DuplicateHandle` 修复 | A0 全绿 |
| M25-1 | **发送路径改造**：每端专职发送线程 + 三级优先 + 服务端 `SO_SNDTIMEO`。**不带任何文件功能** | A3 / A4 / A1 |
| M25-2 | 协议字节层 + `common/transfer.*` 引擎 + `FsList` | 双端单测 + A11 |
| M25-3 | 控制端「文件传输」按钮 + 双栏面板 + 队列（显式传输先跑通，最用户可见的一跳） | A5 的下载半边 |
| M25-4 | 文本/图片剪贴板双向同步 | A7 / A12 |
| M25-5 | 控制端文件惰性粘贴（provider） | A5 半边 |
| M25-6 | 被控端 provider（托盘代理扩）+ R1/R3 定案 | A2 复跑 + A5 整条 |
| M25-7 | 方向可配置 + 配置窗/`--set-server` + 文档/出包 | A6 + 既有 M21 判据复跑 |

## 14. 文档同步清单（实现时执行，本轮不动）

- `README.md`：`## 核心特性` 加一行；`## 通信协议` 消息表补 0x18~0x20。
- `packaging/release-notes.md`：写"暂不支持文件夹 / 无审计日志 / 被控端不可中止"。
- `TASKS.md`：新开 M25 块，按 §13 逐条记判据。
- 《单向网络反向远程控制工具产品需求文档（PRD）.md》：§4.2 增补能力项，§8 把"文件传输"从 P2 提到本期。
- `DEVELOPMENT.md:455` 那条 `- [ ] Clipboard synchronization` 实现完成后勾选。
