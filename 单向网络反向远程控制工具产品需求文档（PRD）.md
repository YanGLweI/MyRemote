# 单向网络反向远程控制工具产品需求文档（PRD）

## 1\. 项目概述

### 1\.1 项目背景

现有主流远程控制工具（向日葵、ToDesk、原生RDP、VNC等）均依赖**控制端可主动访问被控端网络**的双向网络连通能力，无法适配单向隔离内网场景。在企业隔离内网、涉密内网、工业内网等特殊环境中，普遍存在网络单向访问限制，常规远程工具完全失效，因此需要定制开发一套**基于客户端主动反向连接**的专属远程控制工具。

### 1\.2 核心定位

本工具为**单向网络专属内网远程控制工具**，全程由被控客户端主动发起网络连接，服务端无需、也无法主动访问客户端内网，完美适配单向隔离网络环境，实现服务端对多客户端的选择式远程控制。

### 1\.3 适用场景

仅适用于**单向网络连通环境**：

- 服务端所在网络 → 客户端所在网络：**完全不通、无路由、无法ping通、无法主动建立任何TCP/UDP连接**（网络硬性限制）

- 客户端所在网络 → 服务端所在网络：**网络通畅，可主动出站访问服务端IP\+端口**

- 客户端无公网访问权限，仅可访问服务端内网地址，纯内网隔离场景

## 2\. 核心网络规则（重点、硬性约束）

本工具所有功能设计、网络架构、通信逻辑**必须严格遵循以下单向网络约束**，禁止出现任何服务端主动访问客户端的网络行为。

### 2\.1 网络拓扑约束

- **禁止链路**：服务端禁止主动向客户端IP发起任何探测、连接、数据请求，网络层面本身不支持该链路，程序层面需彻底规避

- **唯一合法链路**：由客户端主动出站，持续向服务端指定IP、固定端口发起长连接，所有通信隧道由客户端创建、维持、保活

- **通信载体**：所有远程桌面、指令交互、心跳检测、数据传输流量，均复用客户端主动建立的**单向长连接隧道**，无任何反向主动链路

### 2\.2 网络能力要求

- 服务端：仅开放监听端口，被动接收客户端连接，无任何出站访问客户端网络的逻辑

- 客户端：开机自启、后台常驻，自动探测并连接服务端，断网自动重连，全程主动发起通信

- 不依赖公网、不依赖第三方中继服务器、不依赖内网穿透服务商，纯私有化内网通信

## 3\. 产品整体架构

工具采用 **服务端被动监听 \+ 客户端主动注册 \+ 反向隧道远程控制** 架构，分为两个独立程序：

1. **服务端程序（控制端）**：部署于可被客户端访问的内网主机，提供客户端在线列表、远程控制选择、远程桌面展示、操作交互能力

2. **客户端程序（被控端）**：部署于隔离内网主机，后台静默运行，主动连接服务端，等待被远程控制，无任何主动操作界面

## 4\. 核心功能需求

### 4\.1 客户端核心功能（被控端）

所有网络行为均为**主动出站**，适配单向网络限制

- **自动注册上线**：程序启动后，自动向配置好的服务端IP、端口发起长连接，注册设备信息，上线至服务端列表

- **断线自动重连**：网络波动、服务端重启后，客户端自动重试连接，无需人工干预，保障长期在线

- **后台静默运行**：无前台弹窗、无多余操作，开机自启，隐藏进程运行，适配无人值守设备

- **反向隧道建立**：接收服务端控制指令后，基于已建立的长连接，反向推送本机桌面画面、接收键鼠操作指令

- **设备唯一标识**：客户端绑定设备硬件ID/自定义设备名称，保证服务端可精准区分每台在线设备

### 4\.2 服务端核心功能（控制端）

所有控制行为均基于客户端已建立的隧道，无主动网络访问行为

- **在线设备列表展示**：实时展示所有成功连接的客户端，显示设备名称、设备ID、在线状态、连接时间、内网标识

- **选择式远程控制**：支持手动选中任意一台在线客户端，一键发起远程桌面连接

- **远程桌面交互**：实时查看客户端桌面，支持键鼠操控、窗口缩放、全屏显示、画面画质调节

- **在线状态实时监测**：基于心跳包检测客户端在线状态，客户端断线后自动从列表移除，状态实时更新

- **多客户端并发管理**：支持同时接入多台客户端，支持切换控制不同设备，支持单台/多台设备独立在线

- **安全校验**：支持自定义连接密钥，仅匹配密钥的客户端可接入服务端，防止非法设备接入

## 5\. 详细通信流程（核心开发逻辑）

全程规避服务端主动访问客户端，严格遵循单向网络规则

1. **服务端初始化**：服务端启动，监听固定内网端口，被动等待客户端连接，无任何出站请求

2. **客户端主动连接**：客户端启动，主动出站连接服务端IP\+监听端口，完成密钥校验与设备注册

3. **心跳保活**：客户端定时主动向服务端发送心跳包，维持长连接，服务端被动接收心跳，判断设备在线

4. **发起远程控制**：服务端选中在线客户端，下发控制指令（指令通过已建立的客户端上行隧道下发）

5. **反向数据传输**：客户端接收指令后，主动推送桌面画面数据流，服务端被动接收画面、下发键鼠操作数据

6. **断开控制/断线**：手动结束远程或客户端网络中断，隧道自动释放，客户端自动重试重连

## 6\. 非功能需求

### 6\.1 网络兼容性需求

- 100%适配**服务端不可访问客户端、客户端可访问服务端**的单向隔离网络，无任何网络反向依赖

- 支持纯内网环境运行，无需公网IP、无需端口映射、无需第三方中继

- 适配企业内网、工业控制内网、涉密隔离内网等受限网络环境

### 6\.2 性能需求

- 心跳延迟≤1s，设备上下线状态秒级更新

- 远程桌面低延迟，满足日常运维、设备管控操作需求

- 支持单服务端接入≥50台客户端稳定在线

### 6\.3 安全需求

- 所有通信数据加密传输，防止数据窃听、篡改

- 设备接入密钥校验机制，拒绝非法客户端接入

- 客户端无对外暴露端口，无安全暴露风险，符合内网安全规范

### 6\.4 部署需求

- 绿色免安装，单程序文件运行，部署简单

- 客户端支持开机自启、后台常驻、无界面运行

- 服务端提供可视化操作界面，便捷管理客户端列表与远程操作

## 7\. 禁忌开发规则（强制约束）

开发过程**绝对禁止**出现以下逻辑，违反则无法适配目标网络场景：

- 禁止服务端主动ping、主动连接、主动探测任何客户端IP地址

- 禁止服务端主动发起任何针对客户端的TCP/UDP连接请求

- 禁止采用传统点对点直连、服务端主动寻址的远程架构

- 禁止依赖公网服务器中转、第三方穿透服务

## 8\. 功能优先级

- **P0（必做核心）**：单向反向长连接、客户端自动上线、服务端在线列表、选择式远程桌面控制、断线重连

- **P1（优化功能）**：设备备注命名、画质调节、全屏模式、心跳保活优化

- **P2（拓展功能）**：操作日志记录、多设备批量管理、密码二次校验

> （注：部分内容可能由 AI 生成）
